// for spoofing we may use cleanup API endpoint, need to check permissions
// when it returns customers with overwritten FFs

// or just make it a notepad with possibility to apply spoofing
// it may also check possibility of spoofing and display checkmarks but might be heavy on API

export const SpoofingView = () => {
  return <h1>Spoofing</h1>;
};
